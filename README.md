Project overview
client-side search engine built with vanilla JavaScript that allows users to upload local documents (JSON or TXT), builds a dynamic inverted index in real time, and delivers ranked results featuring typo-tolerant fuzzy search.
